# ReGenNet AI 入口

- 默认使用中文；文档、说明、计划、总结和代码注释都保持中文，除非用户明确要求英文。
- 进入实现前先做 design / plan；新增上下文、决策、取舍和结果写入 `docs/ai/context/`，文件名使用 `YYYYMMDD-HHMMSS-文件名.md`，不要覆写历史文件。
- 长期项目记忆见 `docs/ai/context/20260614-185753-regennet-key-context-extracted-from-agents.md`；阶段性实验和设计以 `docs/ai/context/` 时间戳文档为准。
- 当前主线：InterHuman 150/30/120 two-person motion forecasting；在 ReGenNet 内同口径迁入 SoMoFormer / T2P / JRT 等结构化预测 baseline。
- Label-conditioned two-person forecasting 最初目标是 `前 30 帧双人动作 + 动作标签（如握手） -> 后 120 帧双人动作`；该目标在 NTU120 原始连续帧上受长度分布限制，当前执行协议见下方 NTU120 长度分布条目。详细判断见 `docs/ai/context/20260621-172003-label-conditioned-regennet-forecasting-design-note.md` 和 `docs/ai/context/20260621-172442-three-datasets-action-label-check.md`。
- 导师要求必须使用 ReGenNet 网络时，合理路线是 ReGenNet-style label-conditioned forecasting diffusion：复用条件扩散、timestep embedding、action embedding / classifier-free guidance、双人 motion conditioning 和关系约束；不要把原始 `train/train_mdm.py` 直接当作 30->120 forecasting 入口。
- 动作标签边界：当前本地 InterHuman 只有 actor/reactor 顺序标签，没有“握手”等动作语义标签；NTU120 2P 有 `8: handshaking`，Chi3D 有 `1: Handshake`。不能把 InterHuman 的 actor/reactor 标签当成动作类别监督。
- NTU120 label-conditioned ReGenNet 历史边界：曾计划用 NTU120 2P 全动作类做 `obs30 + label -> future120` 并复用 CMDM 条件扩散；但当前本地 NTU120 H5 严格 `T>=150` 过滤后 train/test 各只剩 5 条且没有 handshaking，不能按该过滤训练全动作类。旧 smoke 和旧最终设计见 `docs/ai/context/20260621-191839-ntu120-label-conditioned-regennet-smoke-design.md`、`docs/ai/context/20260621-191925-ntu120-label-conditioned-regennet-final-design.md`。
- NTU120 长度分布：当前 `xsub.train/test.h5` 中位数约 57/49 帧，`window_len=60` 是不重采样且 train/test 均覆盖 26 类的最大窗口；用户已确认当前执行协议改为 `window_len=60,obs_len=20,pred_len=40`，用于 NTU120 全动作类 label-conditioned ReGenNet/CMDM diffusion smoke 和后续正式训练。统计见 `docs/ai/context/20260622-103022-ntu120-length-distribution-for-forecasting.md`，v2 设计见 `docs/ai/context/20260622-104643-ntu120-label-conditioned-regennet-smoke-design-v2-length60.md` 和 `docs/ai/context/20260622-104723-ntu120-label-conditioned-regennet-final-design-v2-length60.md`；最终模型架构以 `docs/ai/context/20260622-105946-forecasting-cmdm-final-target-architecture-v3.md` 为准，即新建 `ForecastingCMDMDecoder`，用 noisy future40 作为 TransformerDecoder target，用 obs20 token memory + timestep/action token 做 cross-attention 条件。旧 `ForecastingCMDM` Encoder-only 方案仅作为 debug/ablation，不作为最终目标。
- ForecastingCMDM 当前阶段：阶段 A 数据 gate、阶段 B 模型 gate、阶段 C diffusion train gate、阶段 4 sampling/label swap gate 均已通过；阶段 4 结果见 `docs/ai/context/20260622-222109-forecasting-cmdm-phase-4-sampling-label-swap-result.md`。阶段 5 action consistency classifier 已实现，目标是先证明真实 future40 可分类，再决定 generated label consistency 是否可作为指标；实现结果见 `docs/ai/context/20260622-224756-forecasting-cmdm-phase-5-action-classifier-implementation-result.md`。
- 阶段 5 实现入口为 `eval/action_consistency_classifier.py`：只用真实 `future40` 训练 Temporal CNN action classifier，输出 `real_test_metrics.json/confusion_matrix.npy`，并可选对阶段 4 `generated_future40.npy` 输出 `generated_consistency.json`；`valid_for_claim` 必须依赖真实分类 gate 是否通过。实现计划见 `docs/ai/context/20260622-224043-forecasting-cmdm-phase-5-action-classifier-implementation-plan.md`。
- 阶段 6 正式训练设计见 `docs/ai/context/20260623-101205-forecasting-cmdm-phase-6-formal-training-design.md`：目标是训练 ForecastingCMDMDecoder seed0 5000-step 主模型，随后用正式 checkpoint 采样、label swap 和动作一致性复评；视频不属于阶段 6，后置到阶段 7 qualitative visualization。
- 阶段 6 代码准备见 `docs/ai/context/20260623-102141-forecasting-cmdm-phase-6-code-modification-plan.md`：正式采样必须给 `sample/sample_label_forecasting_diffusion.py` 传 `--formal`，使 `metrics.smoke_only=false`；阶段 6 训练/采样/分类器跑完后用 `scripts/check_phase6_formal_outputs.py` 核对 checkpoint、train_log、generated 输出和 `generated_consistency.json`。
- 阶段 6A seed0 5000-step 正式训练已完成，结果见 `docs/ai/context/20260623-103745-forecasting-cmdm-phase-6-formal-training-train-result.md`；最终 checkpoint 是 `save/forecasting/ntu120_label/phase6_cmdm_decoder_len60_o20_p40_h256_l4_s0_5000/model000005000.pt`，train_log 5000 行且 loss finite。
- 阶段 6 完整工程闭环已通过，结果见 `docs/ai/context/20260623-104211-forecasting-cmdm-phase-6-formal-training-result.md`：6B DDIM50 label swap 采样 finite 且非完全相同，6C 真实 future40 分类器 gate 通过（top1=0.8595、top5=0.9697、handshaking=0.9118），完整检查脚本 pass=true；但 generated consistency_acc=0.0，说明当前 seed0 5000-step 生成结果没有被分类器判成输入条件标签，不能写成“动作标签语义控制成功”。
- 阶段 6 拟合诊断见 `docs/ai/context/20260623-105402-forecasting-cmdm-fit-diagnosis-result.md`、`docs/ai/context/20260623-105657-forecasting-cmdm-fit-fix-plan.md`、`docs/ai/context/20260623-110056-forecasting-cmdm-phase-6d-distance-eval-result.md` 和 `docs/ai/context/20260623-110337-forecasting-cmdm-fit-training-switches-result.md`：训练端确实通过 MSE 反向传播学习 `q_sample(real_future,t)->real_future`，teacher-forced/one-step high-noise 预测已优于 copy-last；但 DDIM50 / p_sample_loop free sampling 与真实 future 的距离只接近 copy-last，核心问题是训练目标与最终多步采样目标不完全对齐，后续主指标必须使用 `generated_future40 vs real_future40` 距离评估，分类器一致性只能作为辅助；训练脚本已新增 `--timestep_sampling high_noise`、`--high_noise_min_t`、`--one_step_noise_prob`、`--one_step_t`，默认关闭以保持阶段 6 复现。
- 阶段 6D high-noise/one-step 微调监控见 `docs/ai/context/20260623-110716-forecasting-cmdm-high-noise-finetune-monitor-plan.md` 和 `docs/ai/context/20260623-111818-forecasting-cmdm-high-noise-finetune-monitor-result.md`：从阶段 6 `model000005000.pt` 继续微调，`high_noise_min_t=750, one_step_noise_prob=0.5, lr=5e-5`；当前最佳最终生成 checkpoint 是 `save/forecasting/ntu120_label/phase6d_high_noise_onestep_ft_h256_l4_s0_5100/model000005500.pt`。DDIM50 full test 从 5000 的 MSE=0.036687691/MAE=0.092765272 改善到 5500 的 MSE=0.030736648/MAE=0.083149877；MSE 已明显优于 copy-last 0.035849255，但 MAE 仍高于 copy-last 0.079228513。继续到 5600 出现反弹，不应沿 p=0.5 硬训。
- 阶段 7 最佳 checkpoint 三色 skeleton 结果见 `docs/ai/context/20260623-115241-forecasting-cmdm-phase7-best-checkpoint-tricolor-plan.md` 和 `docs/ai/context/20260623-115916-forecasting-cmdm-phase7-best-checkpoint-tricolor-result.md`：用最佳 `model000005500.pt`、真实 source label、DDIM50 生成 8 个 case，输出目录为 `results/forecasting/ntu120_label/phase7_best5500_tricolor_skeleton_videos`；颜色固定为蓝色 input obs20、橙色 generated future40、绿色 real future40。当前可视化是 `[56,6,T] -> SMPL-X 55-joint` 的单 skeleton，不声明双人互动视频或动作语义控制成功。微调训练使用完整 `T>=60` 训练子集，`kept_count=1956`，26 个标签均覆盖，但类别不均衡，最少类只有 2 条。
- 阶段 7 视频生成设计见 `docs/ai/context/20260623-105106-forecasting-cmdm-phase-7-video-generation-design.md`：第一版用阶段 6 generated_dir 生成 SMPL-X 55-joint skeleton mp4；当前 `[56,6,T]` 输出可用 `Rotation2xyz_x(..., num_person=1)` 转成 `[55,3,T]`，但没有显式双人维度，不能声称双人互动视频。视频只作为 qualitative/debug 材料，尤其要保留 generated consistency_acc=0.0 的解释边界。
- 2026-06-23 用户重新确认视觉拟合硬边界：必须进入 xyz/skeleton space，必须是真双人互动，必须超过 copy-last；如果视频连 obs 最后一帧延续都做不好就没有意义。新设计和实现见 `docs/ai/context/20260623-140232-ntu-two-person-xyz-visual-fitting-redesign-plan.md` 和 `docs/ai/context/20260623-141030-ntu-two-person-xyz-redesign-implementation-result.md`。关键纠正：NTU `[T,56,6]` 应按双人 rotvec/translation 理解，`0:3` 为 person1、`3:6` 为 person2；正确转换是 `Rotation2xyz_x(..., pose_rep="rotvec", num_person=2)` 得到 `[B,T,2,55,3]`。旧阶段 7 的 `pose_rep=rot6d,num_person=1` 单 skeleton 口径不能再作为最终路线。
- NTU 双人 xyz 新入口：`utils/ntu_smplx_2p_xyz.py`、`model/forecasting_ntu_xyz.py`、`eval/eval_ntu_label_xyz.py`、`train/train_ntu_label_xyz.py`、`scripts/build_ntu_label_xyz_cache.py`。正式训练前应先构建完整 xyz cache；评估必须同时报告 `model_metrics` 和 `copy_last_metrics`，以 `xyz_mse/xyz_mae/mpjpe/first_step_error` 等指标判断是否真正超过最后一帧延续。
- NTU 双人 xyz GPU 训练和视频结果见 `docs/ai/context/20260623-142403-ntu-two-person-xyz-gpu-training-video-result.md`：此前 GPU 不可用是工具受限环境未暴露 `/dev/nvidia*`，切换后 RTX 3080 可用。完整 cache 已生成到 `results/forecasting/ntu120_label/xyz_cache_len60_o20_p40`；1000-step checkpoint 为 `save/forecasting/ntu120_label/xyz_transformer_len60_o20_p40_h256_l3_s0_1000/model000001000.pt`。full test: model MSE=0.031281803 vs copy=0.057248837，MAE=0.091381698 vs copy=0.120007492，MPJPE=0.189595376 vs copy=0.260575039，first_step_error=0.0。双人三色视频输出在 `results/forecasting/ntu120_label/xyz_transformer_1000_tricolor_2p_videos`；8 样本子集仅作人工观察，其中 MAE/MPJPE 不整体优于 copy-last，不能拿 8 样本视频单独声称全局视觉拟合成功。
- 2026-06-23 用户发现双人三色视频 Z 轴方向倒置，已只在渲染显示坐标上翻转竖直轴，结果见 `docs/ai/context/20260623-142650-ntu-two-person-xyz-video-z-axis-flip-render-plan.md` 和 `docs/ai/context/20260623-142819-ntu-two-person-xyz-video-z-axis-flip-render-result.md`。后续默认查看 zflip 版本：`results/forecasting/ntu120_label/xyz_transformer_1000_tricolor_2p_videos_zflip`；原始 xyz、checkpoint 和指标不变。
- 论文表述保持保守：不要声称 multi-person forecasting、interaction-aware 或 explicit relation 首创；relation-aware 只表述为相对 concat / parameter-matched concat 有稳定收益，不能写成全面优于 independent。
- 入口内容沉淀见 `docs/ai/content/20260621-172802-agents-entry-content.md`；后续具体实验记录不要继续堆到本文件。
