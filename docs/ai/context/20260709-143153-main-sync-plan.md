# main 分支同步计划

## 当前状态

当前分支：

```text
main
```

上游跟踪：

```text
human/main
```

当前存在未提交改动，主要是 NTU 双人 xyz forecasting 相关代码、评估脚本、可视化脚本和阶段性上下文文档。

## 操作计划

1. 保留现有工作区改动，不回滚用户已有修改。
2. 将当前未提交文件统一提交到 `main`。
3. 拉取/核对个人远端 `human/main` 状态。
4. 如果远端无冲突，将本地 `main` 推送到 `human/main`。

## 边界

远端列表中没有名为 `personal` 的 remote。当前 `main` 跟踪的是 `human/main`，因此本次按 `human` 远端作为 personal 同步目标处理。
