# AGENTS 内容沉淀与压缩计划

## 目标

- 将当前 `AGENTS.md` 的项目入口内容完整沉淀到 `docs/ai/content/`，避免入口文件承载过多阶段性上下文。
- 压缩 `AGENTS.md`，只保留高频稳定规则、项目记忆索引和当前最关键指针。

## 设计

- 新建 `docs/ai/content/` 作为入口内容归档目录；该目录用于保存从 `AGENTS.md` 抽出的原文或结构化整理内容。
- 新增一份带时间戳的归档文档，完整保存本次修改前 `AGENTS.md` 的内容。
- `AGENTS.md` 改为短入口：
  - 保留中文优先、实现前 design/plan、上下文落盘等稳定协作规则。
  - 指向长期项目记忆和本次沉淀文档。
  - 保留当前主线与论文表述边界，删除具体 baseline 完成记录与训练细节，改由上下文文档承载。

## 取舍

- 不删除历史 `docs/ai/context/` 文件，不重命名旧文件。
- 不触碰与本任务无关的模型、训练或实验代码。
- 不把所有历史上下文重新整理成单一总文档，避免制造额外信息漂移；本次只沉淀当前 `AGENTS.md` 的入口内容。
